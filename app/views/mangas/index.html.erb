
<div class = "container">
  <div class="cards">
  <% @mangas.each do |element| %>
    <div>
      <div class="illustration", style="background-image: url('<%= cl_image_path element.photo.key, crop: :fill %>')">
        <a href="<%= manga_path(element) %>", style="display: block;width: 100%;height: 100%;">
          <div class="fading">
            <div>
              <h2><%= element.saga %></h2>
              <h3> n°<%=element.volume%></h3>
            </div>
            <p> <%= element.updated_at.strftime("%d/%m/%Y") %></p>
          </div>
        </a>
      </div>
    </div>

  <% end %>
  </div>









</div>
